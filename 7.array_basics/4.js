// 4. Use a loop to iterate through an array, printing each element to the screen on its own line

var arr = ["Tony", 6, true, 8.8];
var tonyLength = arr.length;

for(var i = 0; i <= tonyLength - 1; i++){
	console.log(arr[i]);
}