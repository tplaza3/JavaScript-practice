
//that take in three parameters. Inside the declared function, multiply the numbers together into one product, and return the result. You may use whatever parameter and variable names you’d like.
function multiplyTrio(a, b, c){
  return a*b*c;
}

//call function
multiplyTrio(4, 8, 10);

//maximum function

function maxOf2 (a, b) {
  if (a>=b){
    return a;
  } else {
    return b;
  }
}